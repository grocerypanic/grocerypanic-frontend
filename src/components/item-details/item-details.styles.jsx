import styled from "styled-components";

export const TabBox = styled.div`
  .nav-rtl {
    padding-left: 40px;
    padding-right: 0px;
  }

  .nav-rtl li {
    float: right;
  }
  min-width: 220px;
  max-width: 402px;
`;
