import styled from "styled-components";

export const PaginationControl = styled.div`
  .pagination {
    margin-bottom: 0px;
  }
  .page-link {
    padding-top: 2px;
    padding-bottom: 2px;
    cursor: pointer;
  }
`;
